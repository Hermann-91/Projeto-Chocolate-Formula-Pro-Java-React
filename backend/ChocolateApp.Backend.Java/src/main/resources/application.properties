# ========================================================================
# ✅ CONFIGURAÇÃO FINAL USANDO O BANCO DE DADOS INTERNO DO RENDER
# ========================================================================

# URL de conexão para o banco de dados interno do Render
spring.datasource.url=jdbc:postgresql://dpg-d3e932ur433s73ed754g-a:5432/chocodatebase?sslmode=require

# Credenciais do seu novo banco de dados
spring.datasource.username=chocodatebase_user
spring.datasource.password=nXKPQ6yEe3IehObMJEhez0LRySSaNrR7

# Driver do PostgreSQL
spring.datasource.driver-class-name=org.postgresql.Driver

# Otimizações do Hikari para o ambiente do Render
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=600000
spring.datasource.hikari.maximum-pool-size=7
spring.datasource.hikari.minimum-idle=2

# Configuração JPA
# Use 'update' para que o Hibernate crie as tabelas no banco novo automaticamente
spring.jpa.hibernate.ddl-auto=update
# Informa ao Hibernate para gerar SQL compatível com PostgreSQL
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true